# LaTeX intermediates
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Built PDFs are tracked manually only at submission milestones
main.pdf

# Experiment results (generated; not source)
experiments/results/

# Verifier JSON manifests are generated; not source
verify_b_t1.json
verify_b_t2.json
verify_b_optional.json

# External audit driver outputs (regenerable; not source)
audit/external_audit/

# Editor / OS noise
.DS_Store
*.swp
